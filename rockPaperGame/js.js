   // if (isNotAvailable && roomsID.length < 2) {
        //     roomsID.push(socket.id);
        //     console.log("Rooms person joined 2 : ", roomsID);
        //     let roomObj = { room: roomName, id: roomsID };
        //     roomObject.push(roomObj);

        //     socket.join(roomName);

        //     socket.emit("joinSuccess", roomObject);

        //     callback("success");
        // }